# Backend configuration file for OpenTofu/Terraform
# Copy this file to backend.hcl and update with your actual values
# Then run: tofu init -backend-config=backend.hcl

bucket         = "your-terraform-state-bucket"
key            = "ridelines-hub/terraform.tfstate"
region         = "us-west-2"
dynamodb_table = "your-terraform-locks-table"
encrypt        = true
